const { Router } = require("express");
const controller = require("./controller");

const router = Router();

// CREATE routes

// READ routes

// UPDATE routes

// DELETE routes

module.exports = router;
